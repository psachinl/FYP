function active_batch(number_of_runs)
    % Function to run active algorithm simulations
    for run=1:number_of_runs
        fprintf('\n')
        fprintf('\n')
        fprintf('Active batch - Run %d \n',run)
        active
    end
end
